#ifndef FASTMATH_H
#define FASTMATH_H

/* Adds two integers using assembly */
int asm_add(int a, int b);

/* Subtracts two integers using assembly */
int asm_sub(int a, int b);

/* Factorial using recursive assembly */
int asm_factorial(int n);

#endif
